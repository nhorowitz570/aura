# Aura — Agent Prompts

## Phase 1: Foundation & Core

```
Build Phase 1 of the Aura health app by following IMPLEMENTATION_PLAN.md in the project root.

Phase 1 covers: Next.js 15 project setup, Tailwind 4.3 design system (dual themes: Violet Aurora + Cyber Teal), all UI primitives (glassmorphism cards, animated counters, progress rings, etc.), hybrid layout (sidebar on desktop, bottom tabs on mobile), particle background animation, Supabase auth with OAuth, multi-step signup with biometrics onboarding, the home dashboard with activity rings and charts, and all 6 database migrations + seed data.

Use the Task Tracker at the bottom of the plan — mark items [/] when in progress and [x] when done. Run all Phase 1 verification checks before finishing. The app must build with zero errors.
```

---

## Phase 2: Feature Modules

```
Build Phase 2 of the Aura health app by following IMPLEMENTATION_PLAN.md in the project root. Phase 1 is complete.

Phase 2 covers: workout logger (exercise library + sets/reps tracking), AI-powered nutrition tracker (Cal AI-style food photo scanning via OpenRouter Gemini 3.5 Flash, editable scan results, AI macro goal generator using Mifflin-St Jeor), sleep tracker, hydration tracker with animated fill visualization, vitals monitoring, and the scan-food + generate-macro-goals Supabase Edge Functions.

Use the Task Tracker at the bottom of the plan — mark items [/] when in progress and [x] when done. Run all Phase 2 verification checks before finishing. The app must build with zero errors.
```

---

## Phase 3: Gamification & Polish

```
Build Phase 3 of the Aura health app by following IMPLEMENTATION_PLAN.md in the project root. Phases 1 and 2 are complete.

Phase 3 covers: XP/leveling system, achievement badges with unlock animations, streak flame animation, weekly challenges with countdowns, personal bests leaderboard, body composition tracking with progress photo uploads (Supabase Storage, 10MB max), settings page, and all three remaining Edge Functions (calculate-xp, check-achievements, daily-summary). Then do final polish: skeleton loading screens, error boundaries, empty states, View Transitions verification, performance audit (Lighthouse 90+), mobile optimization, and accessibility (prefers-reduced-motion).

Use the Task Tracker at the bottom of the plan — mark items [/] when in progress and [x] when done. Run all Phase 3 verification checks before finishing. The app must build with zero errors.
```
